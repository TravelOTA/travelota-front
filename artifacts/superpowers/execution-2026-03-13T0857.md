# Execution Log

## Fase 2: Modularización de Landing

### Paso 1 — Crear LandingHero.vue
- **Files:** `app/components/landing/LandingHero.vue` (nuevo)
- **Cambios:**
  - Extraído Hero section + Login card + Role simulator de `index.vue`
  - Mueve estado `form` y `useAuth` al componente
- **Verificación:** `pnpm lint` → PASS

### Paso 2 — Crear LandingServices.vue
- **Files:** `app/components/landing/LandingServices.vue` (nuevo)
- **Cambios:** Extraída sección "Incoming Service" (3 cards estáticas)
- **Verificación:** Sin lógica JS — componente puro de presentación

### Paso 3 — Crear LandingMap.vue
- **Files:** `app/components/landing/LandingMap.vue` (nuevo)
- **Cambios:** Extraída sección de países con `v-for` sobre array de países
- **Verificación:** `pnpm lint` → PASS

### Paso 4 — Crear LandingBedBank.vue
- **Files:** `app/components/landing/LandingBedBank.vue` (nuevo)
- **Cambios:** Extraída sección Bed Bank (lista features + imagen)
- **Verificación:** Sin lógica JS — componente puro de presentación

### Paso 5 — Refactorizar index.vue
- **Files:** `app/pages/index.vue` (modificado)
- **Cambios:**
  - De 471 líneas → 15 líneas
  - Eliminados imports redundantes (`reactive`, `useAuth`)
  - Añadido `lang="ts"` al script setup
  - Compone: `<LandingHero>`, `<LandingServices>`, `<LandingMap>`, `<LandingBedBank>`
- **Verificación:** `pnpm lint` → PASS, `pnpm lint:fix` aplicado

### Paso 6 — Verificación
- `pnpm lint` → ✅ PASS (0 errores)
- `pnpm typecheck` → Errores pre-existentes en otros archivos del proyecto (no relacionados con Fase 2)
  - Los errores en `LandingHero.vue` (color="blue/amber/red", UCard :ui padding, UCheckbox :ui rounded) son heredados del código original de `index.vue`
  - Errores en otros archivos (`CheckoutPassengers`, `VoucherPreviewModal`, `HotelMap`, etc.) son pre-existentes a esta fase

### Resultado
- **Apariencia visual:** Sin cambios (refactor puro, mismo HTML/CSS)
- **Componentes creados:** 4 en `app/components/landing/`
- **index.vue:** Reducido de 471 a 15 líneas

---

## Fase 3: Formularios y Validación

### Paso 1 — Crear LoginForm.vue
- **Files:** `app/components/auth/LoginForm.vue` (nuevo)
- **Cambios:**
  - Extraído UCard + form de login de `LandingHero.vue`
  - `UForm` con `:schema="loginSchema"` y `:state="form"` — validación Zod en submit
  - `UFormField name="email"` y `name="password"` muestran errores automáticamente
  - `useAuth` llamado internamente (botones simulador de roles auto-contenidos)
- **Verificación:** `pnpm lint:fix` → PASS

### Paso 2 — Crear RegisterForm.vue
- **Files:** `app/components/auth/RegisterForm.vue` (nuevo)
- **Cambios:**
  - Extraído formulario completo de `register.vue` (9 campos + checkbox privacidad)
  - `UFormGroup` → `UFormField` con `name` por cada campo — conecta errores Zod
  - `UForm` con `:schema="registerSchema"` valida en submit
  - Estilos scoped (placeholder uppercase, btn-registro) movidos desde `register.vue`
- **Verificación:** `pnpm lint` → PASS

### Paso 3 — Refactorizar LandingHero.vue
- **Files:** `app/components/landing/LandingHero.vue` (modificado)
- **Cambios:**
  - De ~200 líneas → 42 líneas
  - Eliminados `form`, `handleLogin`, imports de `reactive` y `useAuth`
  - Reemplazado UCard inline por `<AuthLoginForm />`
- **Verificación:** Sin lógica JS — componente puro de presentación

### Paso 4 — Refactorizar register.vue
- **Files:** `app/pages/register.vue` (modificado)
- **Cambios:**
  - De 313 líneas → 20 líneas
  - Eliminados script, form state, paises array, submitRegister, estilos scoped
  - Reemplazado form inline por `<AuthRegisterForm />`
- **Verificación:** `pnpm lint` → PASS

### Resultado
- `pnpm lint` → ✅ PASS (0 errores)
- **Componentes creados:** `app/components/auth/LoginForm.vue`, `app/components/auth/RegisterForm.vue`
- **Validación Zod activa:** errores de schema se muestran inline vía `UForm` + `UFormField`
- **Gate verificado:** submit bloqueado hasta que pasen las reglas de `loginSchema` / `registerSchema`

---

## Fase 4: Experiencia "WOW" e Interactividad

### Paso 1 — Crear InteractiveMap.vue
- **Files:** `app/components/landing/InteractiveMap.vue` (nuevo)
- **Cambios:**
  - Mapa interactivo con `@vue-leaflet/vue-leaflet` + tiles CartoDB Voyager (sin labels)
  - 11 `LCircleMarker` con `LTooltip` para cada país con oficina TravelOTA
  - `ClientOnly` con skeleton fallback para SSR
- **Verificación:** Sin lógica compleja — auto-importado como `<LandingInteractiveMap />`

### Paso 2 — Animar LandingHero.vue
- **Files:** `app/components/landing/LandingHero.vue` (modificado)
- **Cambios:**
  - Import explícito de `Motion` desde `motion-v`
  - Columna izquierda: `initial: { opacity: 0, x: -40 }` → `animate: { opacity: 1, x: 0 }`, duration 0.7
  - Columna derecha (login card): `initial: { opacity: 0, x: 40 }` → `animate`, delay 0.2
- **Verificación:** `pnpm lint` → PASS

### Paso 3 — Animar LandingServices.vue
- **Files:** `app/components/landing/LandingServices.vue` (modificado)
- **Cambios:**
  - Título: fade-down al entrar en viewport (`whileInView`)
  - 3 tarjetas: `initial: { opacity: 0, y: 40 }` → `whileInView`, delays de 0 / 0.15 / 0.30 (stagger)
  - `:viewport="{ once: true }"` en todos
- **Verificación:** `pnpm lint` → PASS

### Paso 4 — Animar LandingMap.vue + integrar mapa
- **Files:** `app/components/landing/LandingMap.vue` (modificado)
- **Cambios:**
  - Título: fade-down en scroll
  - Badges de países: stagger `scale: 0.8 → 1` con delay `index * 0.05` (efecto burst)
  - Reemplazado `UIcon i-lucide-globe-2` por `<LandingInteractiveMap />`
  - Mapa: `scale: 0.95 → 1` + fade al entrar viewport
- **Verificación:** `pnpm lint:fix` → PASS

### Paso 5 — Animar LandingBedBank.vue
- **Files:** `app/components/landing/LandingBedBank.vue` (modificado)
- **Cambios:**
  - Columna izquierda (texto): slide desde la izquierda al entrar viewport
  - Columna derecha (imagen): slide desde la derecha con delay 0.15
- **Verificación:** `pnpm lint` → PASS

### Resultado
- `pnpm lint` → ✅ PASS (0 errores)
- **Archivos nuevos:** `InteractiveMap.vue` — mapa real con 11 marcadores interactivos
- **Animaciones:** todas las secciones landing animadas con `motion-v` (hero al cargar, resto en scroll)
- **Patrón:** `motion-v` `Motion` component con `:while-in-view` + `:viewport="{ once: true }"` para scroll-triggered
- **Gate verificado:** QA visual — animaciones fluidas sin cambios en layout ni contenido

---

## Ejecución — 2026-03-13T0857

**Plan:** artifacts/superpowers/plan-2026-03-13T0831.md (Project Preparation for Backend Integration)
**Rama:** prototype

### Archivos creados
- `app/composables/useApi.ts` — Wrapper centralizado sobre `useFetch`. Inyecta base URL desde `runtimeConfig` y header `Authorization: Bearer` desde cookie `travelota-token`.
- `server/api/agency.get.ts` — Endpoint mock `GET /api/agency` que devuelve los datos de agencia con tipado `Agency`. Listo para reemplazar por llamada real a DB.
- `.agent/workflows/execute-plan.md` — Workflow oficial de ejecución de planes (incluye paso de informe obligatorio).

### Archivos modificados
- `nuxt.config.ts` — Añadido `runtimeConfig.public.apiBaseUrl` (vacío por defecto, override vía `NUXT_PUBLIC_API_BASE_URL` en producción).
- `app/utils/schemas.ts` — Añadidos `agencySchema` + `Agency` type y `itinerarySchema` + `ItineraryPayload` type con validación Zod completa.
- `app/composables/useAgency.ts` — Reemplazado `ref` estático por `useApi<Agency>('/api/agency', { key: 'agency' })`. `computed<Agency>` expone fallback no-nulo mientras carga.
- `app/composables/useItinerary.ts` — Añadidos `fetchItinerary(id)` y `saveItinerary()` que delegan a `GET /api/itineraries/:id` y `POST /api/itineraries`.
- `.agent/rules/git-flow.md` — Añadida regla obligatoria: toda ejecución de plan debe generar `execution.md`.

### Errores encontrados y resolución
- `server/api/agency.get.ts`: `Cannot find module '~/app/utils/schemas'` → el alias `~` en server/ apunta a la raíz, no a `app/`. Fix: import relativo `'../../app/utils/schemas'`.
- `app/composables/useApi.ts`: incompatibilidad de tipos genéricos al combinar `{ headers }` con `...options` en `useFetch` → Fix: `options?: Record<string, unknown>` + `as any` en el objeto pasado a `useFetch`. El tipo de retorno `AsyncData<T | null>` se infiere correctamente.
- `app/composables/useAgency.ts`: `default` factory tipada incorrectamente por TypeScript → Fix: eliminado `default`, usada `computed<Agency>(() => data.value ?? defaultAgency)` para garantizar tipo no nulo.

### Verificación final
- `pnpm lint`: ✅ PASS (0 errores)
- `pnpm typecheck`: ✅ Sin errores nuevos. Errores pre-existentes en `auth/`, `b2b/` y `@internationalized/date` no son parte de este plan.
